// Driver method
//const config = require('../forloop_Program/Utility');
    let i,fact=1,num;
    //console.log(config);
    num = 5
    for(i=1;i<=num;i++){
        fact=fact*i;
    }
    console.log("Factorial of " + num + " is " +fact);